import React from 'react'
import "../assets/styles/MenuBar.scss"
const MenuTitle = () => {
    return (
        <div className={"title"}>
            テンプレくん
        </div>
    )
}

export default MenuTitle;